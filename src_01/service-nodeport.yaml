apiVersion: v1
kind: Service
metadata:
  name: app-nodeport-service
spec:
  type: NodePort
  selector:
    app: multi-container
  ports:
    - name: nginx-external
      protocol: TCP
      port: 80
      targetPort: nginx-port
      nodePort: 30080  
