# Домашнее задание к занятию «Резервное копирование баз данных»

---

### Задание 1. Резервное копирование

### Кейс
Финансовая компания решила увеличить надёжность работы баз данных и их резервного копирования. 

Необходимо описать, какие варианты резервного копирования подходят в случаях: 

1.1. Необходимо восстанавливать данные в полном объёме за предыдущий день.

1.2. Необходимо восстанавливать данные за час до предполагаемой поломки.

1.3.* Возможен ли кейс, когда при поломке базы происходило моментальное переключение на работающую или починенную базу данных.

*Приведите ответ в свободной форме.*

#### Ответ:

1.1 Необходимо восстанавливать данные в полном объёме за предыдущий день.

- Полное резервное копирование (Full Backup) – ежедневное создание полной копии базы данных.

- Дифференциальное резервное копирование (Differential Backup) – если полные бэкапы делаются реже (например, раз в неделю), а дифференциальные – ежедневно (содержат изменения с момента последнего Full Backup).

1.2 Необходимо восстанавливать данные за час до предполагаемой поломки.

- Инкрементное резервное копирование (Incremental Backup) – бэкапятся только изменения с момента последнего Full/Incremental Backup.

- Транзакционные логи (Transaction Log Backup) – если СУБД поддерживает (например, SQL Server, PostgreSQL), то можно делать частые бэкапы логов (каждые 15-30 мин или час).

1.3 Возможен ли кейс, когда при поломке базы происходило моментальное переключение на работающую или починенную базу данных.

Да, такое возможно с использованием:

- Репликации (Master-Slave, Master-Master) – данные автоматически копируются на standby-сервер, который может мгновенно заменить основной при отказе.

- Кластеризации (например, PostgreSQL с Patroni, MongoDB Replica Set) – автоматический failover при падении основной ноды.

- Отказоустойчивые решения (SQL Server Always On, Oracle Data Guard) – обеспечивают автоматическое переключение на резервный сервер.
- 
---

### Задание 2. PostgreSQL

2.1. С помощью официальной документации приведите пример команды резервирования данных и восстановления БД (pgdump/pgrestore).

Команда на создание резервной копии из офф. документации указана следующая:
#### Скриншот 01:

![01](https://github.com/yojihc/netology-Virtualization/blob/12-08_SQL_Backup/pics/01.png?raw=true)

Команда на восстановление резервной копии из офф. документации указана следующая:
#### Скриншот 02:

![02](https://github.com/yojihc/netology-Virtualization/blob/12-08_SQL_Backup/pics/02.png?raw=true)

2.1.* Возможно ли автоматизировать этот процесс? Если да, то как?

Автоматизировать возможно, вот некоторые примеры:

Вариант 1:
    Написать скрипт на создание резервной копии, и добавление этого скрипта в cron

    На просторах интернета нашел готовый скрипт:

```bash
#!/bin/bash
DATE=$(date +%Y-%m-%d_%H-%M)
BACKUP_DIR="/backups/postgres"
USER="postgres"
DB="my_database"

pg_dump -U $USER -F c -f $BACKUP_DIR/${DB}_${DATE}.dump $DB
find $BACKUP_DIR -type f -mtime +7 -delete  # Удаляем старые бэкапы (старше 7 дней)
```


*Приведите ответ в свободной форме.*

Вариант 2:

Готовые инструменты по типу  pgBackRest, Barman, WAL-G. 

Вариант 3:

Облачные решения, В яндексе облаке нашел "Резервные копии в Managed Service for PostgreSQL"

---

### Задание 3. MySQL

3.1. С помощью официальной документации приведите пример команды инкрементного резервного копирования базы данных MySQL.

#### Скриншот 03:

Скриншот с примерами из офф. документации.

![03](https://github.com/yojihc/netology-Virtualization/blob/12-08_SQL_Backup/pics/03.png?raw=true)

---

3.1.* В каких случаях использование реплики будет давать преимущество по сравнению с обычным резервным копированием?
*Приведите ответ в свободной форме.*

#### Ответ:

Репликация лучше обычных резервных копий, когда нужна максимальная доступность и минимальное время на восстановление.



---

Задания, помеченные звёздочкой, — дополнительные, то есть не обязательные к выполнению, и никак не повлияют на получение вами зачёта по этому домашнему заданию. Вы можете их выполнить, если хотите глубже шире разобраться в материале.
